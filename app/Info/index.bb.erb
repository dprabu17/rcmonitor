<div data-role="page">

  <div data-role="header" data-position="inline">
    <%= link_to "New", :action => :new %>
    <h1>Infos</h1>
    <%= link_to "Home", RhoConfig::start_path %>
  </div>

  <div data-role="content">
    <table>
      <tr>
      <% @infos.each do |obj| %>
        
            <td class="recordLabel"><%= link_to "#{obj.available}", :action => :show, :id => obj.object %></td>
        
      <% end %>
      </tr>
    </table>
  </div>

</div>

<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @device.name %></h1>
    <a href="<%= url_for :action => :show, :id => @device.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @device.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @device.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="device[name]" class="fieldLabel">Name</label>
            <input type="text" id="device[name]" name="device[name]" value="<%= @device.name %>" <%= placeholder( "Name" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
